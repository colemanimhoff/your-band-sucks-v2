import React from 'react'

import AppBar from '../components/AppBar'

const Albums = () => {
    return (
        <div>
            <AppBar />
            <h1>Albums</h1>
        </div>
    )
}

export default Albums